import breakpoints from './theme/breakpoints'
import colors from './theme/colors'
import fontSizes from './theme/fontSizes'
import spacing from './theme/spacing'

import { buttonStyles as buttons, buttonSizes } from './theme/buttons'

const theme = {
    colors,
    spacing,
    fontSizes,
    breakpoints,
    buttons,
    buttonSizes,
}

export { buttonDefaults } from './theme/buttons'
export { buttonSize } from './theme/buttons'

export default theme
